/**
 * Stack implementation using JavaScript arrays.
 * @author antoinette@primeacademy.io
 */

 function Stack() {
 	var elements = [];

 	/**
 	 * Adds one element to top of stack.
 	 *
 	 * @param an element
 	 */
 	this.push = function(element) {
 		return null;
 	}
 	/**
 	 * Removes top item from stack.
 	 * Returns removed element.
 	 *
 	 * @return removed element
 	 */
 	this.pop = function() {
 		return null;
 	}
 	/**
 	 * Returns top element of stack.
 	 * Does NOT remove element.
 	 *
 	 * @return top element of stack
 	 */
 	this.peek = function() {
 		return null;
 	}
 	/**
 	 * Determines if stack has elements.
 	 *
 	 * @return true if no elements, false if 1 or more elements
 	 */
 	this.isEmpty = function() {
 		return null;
 	}
 	/**
 	 * Removes all elements of stack.
 	 */
 	this.clear = function() {
 		return null;
 	}
 	/**
 	 * Determines number of elements in stack.
 	 *
 	 * @return number of elements in stack
 	 */
 	this.size = function() {
 		return null;
 	}
 }